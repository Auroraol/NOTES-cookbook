+ 联合体声明：
   union one4all
  {
      int n;
      char character;
      double f;
  }; name   // 可以在声明的同时创建对象（name），一般在union是嵌套时用

+ 匿名联合体
  struct weight
  {
      int n；
      union 
      {  
          int n；
          double d；
      }；
  }；
  .......
  weight p;
  p.n 和 p.d //可以直接访问到联合体的元素


